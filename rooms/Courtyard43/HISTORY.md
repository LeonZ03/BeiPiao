# Courtyard43 · 制作记录

## 2026-09-22：参考与基线检查

读取根目录工程约定、README、房间目录说明和模板，逐张检查 4 张本地照片。用户确认以入住后照片为主，空房照补结构。记录可见关系、未知边界、复用候选和分阶段门槛，见本房间 README。

官方 Blender Lab MCP 只读打开永旺家园当前完整源：米制、`web_revision=sunview34`、16 项外部文件引用无缺失。已核查笔记本、鼠标、帽子、床品、窗帘及柜门枢轴存在。未运行旧房间生成器，未改动其源或导出。

本地 HTTP 网页已查看永旺家园默认闭帘与总览，控制台未见错误。该检查建立观感基准，不代表新房间已建成或通过验收。

原始参考保持忽略、不上传。新房间保持 `pending`；结构确认是精修前的用户门槛。

## 2026-09-22：独立白模与视频修正（whitebox01 → whitebox02）

经官方 Blender Lab MCP 建立独立米制源、阶段脚本、结构参数和 `blender-room-pack-1` 网页包。第一版 73 个体块、7,788 三角形，保存为 Git 检查点 `012e8d6`。当前第二版为 86 个体块、9,096 三角形，`web_revision=courtyard43-whitebox02`。源与导出包来自同一次 MCP 操作，不依赖私人参考或 `analysis/` 才能运行。

补充视频已复制到本房间忽略目录。逐秒观察全片，并放大 0.6、4.2、5.2、6.1、11.6 秒关键帧：补白色双门柜、两侧磨砂板夹中央横格、外窗上横框和右转角玻璃；缩短最初估计的卧室进深，使床长边贴近入口侧墙。初版约 3.35 米进深改为约 2.85 米仅是照片比例假设修正，不是实测。仍以入住后灰窗帘/陈设为后续主参考。

白模网页独立于正式首页，复用 B 方案控件及现有模型解析/下载/进度模块，保持自由探索、总览、复位、沉浸与手机控制。中性光照不声明真实时间/地理朝向；未见部分标浅棕色，暂不建立厨卫、柜内或猜测外景。白模音效按钮禁用并标注阶段状态，尚未伪装成完成房间。

已验证：官方 MCP 重开第一版源（米制、无链接库与外部图片）；当前网页模型法线、UV、索引、无零面积三角形及原始/gzip 一致；阳台通道、床下空隙与床垫/柜体阻挡回归。真实 HTTP 浏览器检查六个初版视角、修正版入口/转向/移动及 390×844、844×390 视口，手机无横向溢出，沉浸保留 48 像素移动/升降键。网页静止时不重复完整渲染。

修正版补验：通过仓库官方 MCP 客户端重新打开第二版源，确认 `courtyard43-whitebox02`、86 个网格、米制、无缺失外部文件。补查阳台转角、反向和总览；浏览器控制台无资源/着色器错误，返回首页后重入成功。构建产物中的新房间清单、原始 binary 和 gzip 与工作源逐字节相同。宿主后台检查未返回时使用同一个官方服务器的仓库 STDIO 客户端完成检查，未切换第三方实现或启动 TCP 控制端口。

永旺家园源、模型包与运行时未修改。`tests/check.ps1`（含 Windows 启动器、加载生命周期、音频、微风、缓存、衣柜、外景）、`npm run test:scene` 和 `npm run test:pages` 已通过；视频修正后的新白模专项检查再次通过。完整成品的材质、运动全程和性能验收须等精修阶段，当前不能据此宣称达到永旺家园完成度。

本轮使用 1 个只读资产/经验核查子智能体，显式启动参数 `gpt-5.6-luna / low`；主智能体核查源、判断结构、执行 MCP 建模和网页验收。子智能体未修改文件。

## 2026-09-22：衣柜侧外扩、清空入口（whitebox02 → whitebox03）

父提交 `4e4c4bf`。用户提供 V02 并指定前 4 秒，明确确认衣柜一侧整体向外推、入口不应摆椅子。参考文件保存在忽略的 `references/`，本轮只观察前 4 秒，不推断其余片段。将右墙、衣柜与门向网页 +X 外移约 0.8 米，卧室宽度估计由 3.25 改为 4.05 米，进深 2.85 米保持不变。外移方向已确认，具体数值仍待白模反馈；不是实测尺寸。

删除错误的门边备用椅，只保留桌前朝向书桌的椅子。左墙、床、书桌和阳台隔断/通行口保持原位置；地面、天花与阳台右边界随房间外扩延伸。采用偏移后的房间中心计算边界，避免对称加宽导致左侧家具被连带移动；网页碰撞范围同步采用同一中心，防止画面加宽但旧边界仍挡路。

官方 Blender Lab MCP 重新生成并导出 `courtyard43-whitebox03`，80 个网格、8,448 三角形；随后同一官方 MCP 重开源工程，确认米制、正确 revision、无外部图片或链接库、无门边椅子。参数保存在 `history/inputs/whitebox-03.json`，生成器保持父 revision 与未确认阶段保护。

检查通过：新房间模型属性/法线/索引/gzip、加宽通道跨过旧右墙、新右墙与柜体阻挡、入口原椅位通行、床下间隙和阳台通道；旧房间源、模型包和主运行时没有改动。对比父清单确认左墙/床/桌/隔断矩阵不变，右墙/柜/门均只平移 0.8 米。已运行新白模专项、房间目录检查、修改脚本语法和 Pages 构建。

真实本地 HTTP 浏览器已核对入口、桌旁向衣柜回看、总览，并实际移动至新增通道、拖动环顾；控制台无新增错误或警告。390×844 手机竖屏无横向溢出，六个方向/升降按钮保持 48×48。仍为待确认结构白模，未进入精修。

## 2026-09-22：纠正桌椅朝向（whitebox03 → whitebox04）

父提交 `9a943c6`。用户再次提供白模与 V02 实拍的对照截图，揭示上一轮只删掉入口椅子、却遗漏桌椅朝向的错误。重新对照 P01、入住后 P03 与 V02：桌子长边应沿阳台这面墙横摆，椅子在桌前靠床一侧，椅背朝床，而非纵向贴床头墙、椅子朝着阳台通道。

通过官方 Blender Lab MCP 将桌子的长边从 Z 改沿 X，椅背由 +X 改到 +Z；桌子靠阳台入口左侧墙，椅子移至桌前靠床侧。左门垛按可见的完整桌宽加宽约 0.3 米，门洞左界从 -0.95 改为 -0.65 米，桌面右端止于 -0.70 米，留约 5 厘米估计间隙。门槛、门楣与左侧收帘体同步适配，右侧隔断位置保持不变。所有数值仍是照片估计；未改卧室宽深、床、衣柜、房门或上一轮外扩。补齐原白模桌腿到桌面、椅背到座面的短缝。

独立源及导出 revision 为 `courtyard43-whitebox04`，80 网格、8,448 三角形。官方 MCP 重开白模源并确认无外部图片/链接库，生成器保持父 revision 保护。新白模专项检查、房间目录、Python/JS 语法与 Pages 构建通过；几何检查涵盖桌子横向长边、椅背朝床、椅子与床分离、桌椅不侵入门口，以及原有阳台通行和床下间隙。对比父版确认床、柜、门、隔断、房间尺度均未变；永旺家园文件未改动。

本地 HTTP 网页已按正常入口视角和桌旁近景直接对照照片，实际旋转并从桌旁沿通道走入阳台 1.8 米，无家具阻挡；控制台无错误/警告。此次验收以桌面长边、椅背方向及右侧门口三者同时对应参考为准，不能把“有一桌一椅”或通用检查通过当作摆放正确。结构仍待用户确认，未进入精修。

## 2026-09-22：床头侧扩宽与家具联动加长（whitebox04 → whitebox05）

父提交 `7dd5c09`。用户用箭头明确指出床头侧应扩宽，床、桌子和桌前那段墙都应对应变长。本轮按 -X 外推约 0.4 米处理：左界从 -1.625 到 -2.025 米，右界仍为 2.425 米，卧室总宽估计 4.45 米；床长由 2.0 到 2.4 米、桌长由 0.9 到 1.3 米，桌前墙段从 0.975 到 1.375 米。方向及联动关系已获用户明确指示，40 厘米幅度与床/桌尺寸仍是白模估计，不是实测或商品规格。

通过官方 Blender Lab MCP 按新尺寸重建床架、床垫、桌面及墙地顶，床头和靠墙支脚同步外移，椅子随加长桌面的中心重新对齐。床尾、桌面右端、阳台门口/隔断、衣柜和房门保持原位。厚度、倒角、床宽、桌深与高度保持不变；未用整体非均匀缩放拉伸厚度或修改相机来制造加宽观感。参数保存于 `history/inputs/whitebox-05.json`，父版本和未确认阶段保护继续有效。

源与导出同次生成 `courtyard43-whitebox05`，仍为 80 网格、8,448 三角形。官方 MCP 重开源确认米制、正确版本、无外部图片或链接库。新白模专项、目录检查、Python/JS 语法及 Pages 构建通过；直接解析父版与本版导出包，核对床/床垫/桌面/桌前墙的左端均外移 0.4 米、右端和厚度不变，房门、衣柜、通道与全部六个相机参数不变。永旺家园无改动。

本地 HTTP 网页在相同入口视角和桌旁近景检查了同步加长效果，并实际移动 1.9 米到旧左墙以外的新增空间，未被旧边界拦住；控制台无错误/警告。尚未确认最终尺度，不进入精修。

## 2026-09-22：衣柜侧小幅回收（whitebox05 → whitebox06）

父提交 `489db15`。用户朝衣柜与门方向截图要求“整体往回收一点点”。将右墙、衣柜含双门与把手、房门及门洞向 -X 内收约 0.2 米，对应墙地顶和阳台右边界同步调整；右界从 2.425 到 2.225 米，总宽估计 4.25 米。床头侧左界仍为 -2.025 米，床长约 2.4 米、桌长约 1.3 米及桌前墙段保留。回收幅度为白模估计，不是实测。

官方 Blender Lab MCP 生成并重开 `courtyard43-whitebox06`：80 网格、8,448 三角形，米制且无外部依赖。新白模专项、目录检查、修改脚本语法和 Pages 构建通过。通道回归的终点改为当前墙内侧，同时继续要求跨过最初右墙位置，保留实体阻挡断言；避免把当前合法墙面误当作回归失败。父版与当前导出包的包围盒比较确认右墙/柜/门仅内移 0.2 米，左墙、床、桌、椅、左门垛、阳台通道与相机不变。永旺家园未修改。

真实本地 HTTP 网页已从桌旁转向衣柜核对回收后的间距，并实际前进至新柜门表面：先移动 1.4 米，再次请求 0.5 米时在约 0.325 米处由柜门阻挡，未穿入柜体；复位入口正常，控制台无错误或警告。结构仍待确认。

## 2026-09-22：结构批准与独立精修（whitebox06 → interior01）

用户明确确认白模没有问题并开始装修，以 `cb6fe56615a53e26810968f6c704c7536ffeba1a` 的 `whitebox06` 为批准基线。结构约束复制为 `history/inputs/approved-layout.json` 并标记批准，原白模源、模型包、历次参数和审阅入口保留；白模生成器在批准后锁止。床长 2.4 米、房间边界及其他尺寸仍为照片估计，批准布局没有改变其证据等级。

通过 Blender Lab 官方 MCP 分别建立建筑、家具、陈设三个独立组件源，再以 `assemble-interior.py` 与 `export-interior.py` 集中生成 `assets/full-room/Courtyard43-interior.blend` 和 `room-site/dist/assets/rooms/Courtyard43/interior/` 的 `courtyard43-interior01` 包。PBR 图像、UV/UV1、多材质、父子层级、窗帘形态键及稳定交互 ID 一同导出，原始/gzip 二进制来自同次导出。完整工程没有覆盖白模或永旺家园。

入住后 P03/P04 为主要外观依据，完成白墙/边顶、红棕木地板、灰帘、封闭阳台、砖纹半墙、磨砂植物纹隔断和青蓝横条罩体；床保留白板床头和细圆腿，桌子沿阳台墙横摆、椅背朝床，入口无备用椅。衣柜为木色侧板与双白门，柜内空柜、单高层板和挂杆是明确标注的最小推定，没有添加镜子、衣物或无证据厨卫。罩体内部帘槽、门五金和局部阳台延续亦保留推定说明。

复用前只读核对永旺 `sunview34` 最终完整源，SHA-256 为 `67765fb8a97bf6b86d9d272f5319ab8aa047f5330438210ccce063a06833e150`，前后保持不变。电脑、鼠标、帽子及枕头采用独立变体；电脑合盖并改浅银外壳，不声称型号一致；帽子移除不可确认的 LA 绣字。花纹图像、棉纱纹理独立复制，新被面按本房尺寸制作。只在目标副本中焊接重复极点/缝端点并去除真正零面积面，保留有效几何断言。道具补齐可见类型的水瓶、杯、纸巾、挂袋、纸篓、小容器和连接电线，未知文字留空；窗外邻楼只作有证据的低细节补全。

桌面网页对照后，家具木纹校准为浅米色低对比 sRGB `#D4C7B2`，边带 `#D0C1A7`；PNG 直接按目标 sRGB 字节编码并读回校验，常量材质只做一次线性解码。床品印花线性尺度缩小 22%，棉纱 UV1 密度不变；被面增加少量定向宽褶、局部浅堆积和不齐垂边。88 个非目标家具网格的世界坐标 hash 保持不变，三层床品与床垫无 BVH 交叉。柜门 25 态检查通过，靠阳台一门限制为 72°、入口一门为 90°，与扩大 20 毫米的暖气台面包络无交叉；最小采样距离约 27.6 毫米。这些是模型检查数值，不是房间测量结果。

灰帘与挂环保留独立闭合/收拢形态键及实体厚度，41 态检查了门垛、罩体、台面和门槛余量。建筑贴图与最终网页地板颜色重新核对色彩空间；地板的顶点、索引及 UV 保持。总览按整组隐藏被剖切墙面上的门和开关五金，避免留下悬浮部件。完整的来源、精确对象名与检查记录见 `docs/ARCHITECTURE.md`、`docs/FURNITURE.md`、`docs/PROPS.md`。

独立成品页 `courtyard43.html` 已接入首页时间线和目录，索引改为 `ready` 并指向精修源/包。自由探索、总览、复位、沉浸、加载/错误状态及手机控件沿用现有体验；灰帘、双柜门、房门、墙壁开关与反锁具有往返状态，默认静音，开启后使用适用音效。未引入持续微风或微尘，静止视角按需重绘。地理朝向、拍摄时刻和未见厨卫仍不作猜测。

Pages 构建改为递归发现各房间及阶段的 `scene.json`，每个包独立生成不超过 8 MiB 的原始/gzip 哈希分块。永旺、Courtyard43 白模与精修三个包的字节一致性、本地回退、下载进度、404/截断错误及缓存规则均通过。最终精修包为 383 个网格、397,540 个三角形、19 个贴图描述符，原始二进制 14,830,088 字节。

最终检查通过：`tests/check.ps1`（含新房间数据/运行时、原房间微风/缓存/衣柜/音频/外景、生命周期和 Windows 启动器）、`tests/scene.mjs`、`tests/pages.mjs`、全部房间 Python 脚本语法。官方 MCP 重开完整源，确认米制、26 项图像引用均位于 Courtyard43 且无缺失，外链对象/材质为零。永旺源 hash 和运行包、主运行时保持不变。

真实 HTTP 浏览器已核对入口、床边、桌面、阳台、反向与总览及物件近景，实际点击窗帘、柜门、房门、灯和反锁；修复房门被相机阻挡后下一次点击无法回到完整端点的问题。锁门、拒绝开启锁住的门、解锁通过。检查 390×844 / 844×390 视口、48 像素移动/升降控件、44 像素功能按钮、沉浸、旋转、全屏返回首页再进入，均无横向溢出；永旺实际重新加载通过。控制台无新增资源、JavaScript、着色器或 WebGL 错误。静止不连续重绘，合批保留交互与剖切对象；这不构成所有设备固定帧率承诺。

本轮实际使用 2 个子智能体，启动参数均显式指定 `gpt-6-astra / xhigh`，分别负责建筑/网页与家具；主智能体负责陈设、合成、浏览器验收与发布。本提交完成本地验收，推送及对应 Pages 部署结果另行核对。

发布核对：装修提交 `7fee5abae7db06eca306540e2be7ff937beb8de3` 已推送至 `main`，同一 SHA 的 `Cloudflare Pages` 检查为 `completed / success`，部署 ID `eea37ca4-dfce-40bb-b072-34917d2b0bf2`。正式站 `https://room.leonz03.dpdns.org/courtyard43.html`、入口、运行脚本和场景清单均正常响应，gzip 分块合并解压后与本地 14,830,088 字节模型完全相同；线上永旺仍为 `sunview34`。内置浏览器公网导航连续超时，因此不把本地网页截图冒充线上截图；线上结论依据精确提交部署状态及实际下载的资源一致性，画面/交互结论依据前述本地 HTTP 实测。
