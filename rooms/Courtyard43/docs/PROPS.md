# Courtyard43 陈设来源

入住后 P03/P04 为主。原图不进入网页资源，文字、商标和不可辨认标签不猜造。通过官方 Blender Lab MCP 执行 `scripts/build-props.py`，保存 `assets/props/Courtyard43-props.blend`，由完整源组装阶段追加。

## 复制变体

来源：`rooms/yongwang-jiayuan/assets/full-room/永旺家园-完整场景.blend`，`sunview34`，SHA256 `67765fb8a97bf6b86d9d272f5319ab8aa047f5330438210ccce063a06833e150`，父检查点 Git `cb6fe56`。这是当前完整工程中的最终资产，未运行旧房间生成器。

| 原对象 | 本房间处理 |
| --- | --- |
| detailed-laptop | 追加完整层级，按原铰链合盖；外壳改为参考图可见的浅银色。仅作为外形基础，不宣称电脑型号相同。 |
| white-gaming-mouse | 保留实体按键、滚轮、曲面；调整桌上朝向，落于有厚度的黑鼠标垫上。 |
| Black LA Baseball Cap | 保留帽冠、真实帽檐厚度、缝线与纽扣；移除不可从参考确认的 LA 绣字，复制三顶深色挂帽。 |

网格、材质和图像在目标文件中独立；所需纹理复制到本组件 `textures/`，不从网页跨房间动态读取可写数据。不开启源房间灯光、相机或动画依赖。源完整工程不回写。

## 新建与推定

暖气罩上纸巾包、少量抽出纸巾、带红色盖的透明水瓶及透明杯依据可见类型建模。瓶体/杯体具有内外壁、底厚和口沿，标签不写未知文字；杯瓶底落在保留的罩台面上，半径不超过 5.5 厘米。物件型号与尺寸为照片估计。

补充 P03 可见的柜侧深蓝/浅色挂袋、奶油色废纸篓与薄内衬、桌上小容器和供电线。挂袋有布层厚度、局部塌陷、两端连入袋口与墙钩的提带；文字留空。电线从墙边插头垂落再上桌，接入电脑侧面，不悬断。纸篓底面接地。所有变体独立清理重复极点和退化面，并核对旋转体正向体积；不修改永旺家园原网格。

窗外仅放有证据的邻近住宅立面与窗格。准确退距、遮挡后的立面与数量是低细节补全，不代表实测外景；不导入永旺家园树木。入户区域没有额外椅子，也不添加无资料的厨卫。

几何与材质最终以完整网页近景验收。组件源之外的输出由 `assemble-interior.py` 和 `export-interior.py` 同一次官方 MCP 导出生成。
