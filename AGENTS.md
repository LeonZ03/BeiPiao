# 北漂居住记录：AI 工程约定

本文件面向继续维护本仓库的 AI。目标是让新的房间复刻沿用已经验证过的建模、导出和网页运行方式，减少结构反复、穿模、材质失真、性能退化和不可复现修改。它不是历史日志；具体版本变化和用户可见说明写入 `room-site/README.md`。

## 1. 基本原则

1. **先证据、后结构、再细节。** 先从照片、视频和用户口述建立平面关系、方向、主要尺寸比例和遮挡关系；结构未确认前，不在小物件上投入精修。
2. **结构确认后保持稳定。** 当前永旺家园的整体布局已获认可。除非用户明确更正，不移动房间边界、门窗、床、衣柜、桌子、卫生间或窗外主体关系。后续优先改善材质可信度、真实厚度、自然曲面和接触关系。
3. **照片推定不是测量。** 没有实测尺寸、多角度照片、扫描或厂商 CAD 时，只能表述为“根据参考图和已知尺度估计”。不得宣称毫米级、测量级、摄影测量级或 1:1 复刻。
4. **网页最终画面才是最终验收对象。** Blender 视口和离线渲染用于检查模型，不代表 Three.js 实时光照、色调、透明、粒子和后处理的最终效果。
5. **每项修改都必须可追溯、可恢复、可验证。** 使用 Git 提交保存可恢复的检查点，不再创建备份压缩包；重要几何修改要有可重复脚本或清楚的源文件；不得只改导出成品而丢失 Blender 源。
6. **不要承诺一次完成所有精度。** 每个阶段只做该阶段需要的最小核对，用客观检查结果推进；不以“看起来应该没问题”代替浏览器实测。

## 2. Blender 强制工作方式

- 所有 Blender 建模、材质编辑、场景检查、渲染和导出，必须使用 **Blender Lab 官方 MCP**。官方来源是 <https://projects.blender.org/lab/blender_mcp>。不得把同名第三方社区插件称为官方版。
- Codex 全局 MCP 名称为 `blender`。工具已暴露时直接调用；未暴露时，通过仓库内 `tools/call-blender-mcp.py` 以标准 MCP 协议调用同一个官方服务器。
- 完成 `tools/README.md` 中的可选建模环境安装并激活该环境后，用该环境的 Python 查询实际工具和参数；网页的 `make init` 不安装 Blender 开发依赖。不要依赖仓库外虚拟环境或记忆猜测：

  ```powershell
  python tools/call-blender-mcp.py --list
  ```

- 优先使用官方 `execute_blender_code_for_cli` 等后台工具。后台工具按需打开 `.blend`，不需要常驻 TCP 控制端口。扩展的自动启动已明确关闭，不得擅自重新开启。
- 建模脚本可保存在 `room-site/tools/`，但必须经官方 MCP 在 Blender 中执行。不要绕过 MCP 直接调用 Blender 可执行文件运行脚本。
- 开发环境使用的官方 MCP Python 依赖固定到 `ff54e4d8f6b09502f2f466189cca0e52b4a91643#subdirectory=mcp`；升级前先验证工具参数和导出结果。不要改用仓库内未审查的第三方同名实现。
- `tools/call-blender-mcp.py` 应通过 `BLENDER_PATH` 或 PATH/标准安装位置发现 Blender。MCP 参数、检查输出和临时报告放在未跟踪的 `analysis/`；不要把盘符、用户名、个人虚拟环境或 Blender 安装路径写进长期脚本和文档。脚本内路径应由仓库根目录或脚本目录推导。

## 3. 权威文件与派生文件

| 内容 | 权威位置 | 说明 |
| --- | --- | --- |
| 当前完整房间 Blender 工程 | `generated-assets/full-room/永旺家园-完整场景.blend` | 当前场景几何、材质、实例、接触关系和交互轴的主要源文件 |
| 独立精修资产源 | `generated-assets/<asset>/` | 例如高达、门饰、头盔、摩托车；应保留 `.blend` 和必要生成记录 |
| 可重复建模/精修脚本 | `room-site/tools/` | `build-*` 创建资产；`refine-*` 针对特定父版本精修；先读脚本头部和 revision 断言 |
| 浏览器最终模型包 | `room-site/dist/assets/full-room/` | `scene.json`、`geometry.bin`、`geometry.bin.gz` 是派生成品，不是唯一源 |
| 浏览器运行时 | `room-site/dist/` | Three.js 场景加载、实时灯光、交互、导航、UI、粒子、水流和后处理 |
| 本地与公网启动器 | `启动房间.cmd`、`local-access/` | Windows 双击入口、本地服务、Cloudflare 临时隧道和健康检查 |
| 可提交的回归测试 | `tests/` | 几何、交互、启动器和浏览器回归；新检查应从任意克隆路径运行 |
| 本地检查报告 | `analysis/` | 未跟踪的临时 MCP 参数和检查输出；报告不是模型源 |
| 版本历史 | Git commits | 通过提交记录追溯和恢复模型、材质、导出包及网页代码；不创建 ZIP/TAR 等备份压缩包 |

处理现有房间时，先读取 `room-site/dist/assets/full-room/scene.json` 的当前 `revision`，不要把本文件中的示例版本当作永远有效。当前完整生成器 `room-site/tools/build-full-room.py` 会重建并覆盖完整工程。完整工程已经经过多轮 MCP 精修后，**禁止直接运行该生成器覆盖当前 `.blend` 和模型包**。如果确实需要从基础版本重建，先通过 Git 提交保存当前工程，再按父 revision 顺序重放所有必要的 `refine-*` 步骤，并逐步验收。

精修脚本里的输入 revision 断言是保护措施。断言失败时先调查当前父版本和变更链；不得删除断言、强行套用到不匹配的场景。对现有工程做了手工或 MCP 编辑后，必须保存源工程并建立 Git 检查点，不能让旧生成脚本覆盖新成果。

## 4. 新房间的推荐分阶段流程

### 4.1 参考资料与约束表

- 按房间和拍摄时间整理参考图，区分入住前、入住后和不同季节；同一物件冲突时明确主参考，不把不同时期物品混成一个“真实瞬间”。
- 记录门窗方向、相机大致位置、墙面颜色、固定家具、可移动家具、主要物件和窗外关系。
- 每个尺寸注明来源：`实测`、`已知商品规格`、`用已知物件换算` 或 `照片估计`。对照片估计保留合理误差，不伪造精确小数。
- 先确定统一坐标、房间原点、米制单位和浏览器朝向，再开始建模。现有导出约定为网页 **X 向右、Y 向上、Z 向前**；Blender 与网页的轴转换必须在导出器中集中处理，不能靠各对象临时旋转凑齐。

### 4.2 结构白模

- 第一阶段只建房间外壳、地面、天花、门洞、窗洞、固定管线，以及床、柜、桌、卫浴等大体块。
- 用入口、床边、书桌、窗边、卫生间和空间总览至少六个视角核对：动线、遮挡、开门/开窗方向、家具间距和人体尺度。
- 在结构通过前，使用中性材质和简单光照；不要用强景深、体积光或暖色滤镜掩盖比例错误。
- 新房间应使用独立的 `.blend`、独立的 `dist/assets/<room-id>/` 和独立 revision，不能覆盖永旺家园资源。

### 4.3 重点物件

- 按视觉显著度排序：轮廓醒目的大物件、靠近默认视点的物件、用户明确强调的纪念物优先。
- 每个重点物件先检查轮廓和整体比例，再做结构连接、材质和标识。只增加局部细节不能弥补错误轮廓。
- 商标、文字和图案只复刻参考图中可确认的可见部分；不可辨认的能耗等级、型号、参数等留空，不编造。
- 隐藏面可合理补全，但要在说明中标注为推定。若使用公开厂商资料，只用来校准可验证的结构或尺寸，并记录来源。

### 4.4 材质、光照与氛围

- 基础色纹理应去除照片中烘死的阴影、褶皱、高光和透视，真实明暗由网格和实时灯光产生。基础色按 sRGB，法线、粗糙度、金属度、AO 和光照贴图按正确线性色彩空间处理。
- UV0 用于物体材质，UV1 用于烘焙或间接光；导出和合批不能丢失 UV1。纹理密度要与真实尺寸匹配，避免墙纸、木纹、布纹和瓷砖缝比例失真。
- 金属、玻璃、陶瓷、布、塑料和木材必须靠合理粗糙度、实体边缘和环境反射区分，不靠极端饱和或镜面值。
- 午后氛围以参考图为准。永旺家园当前基准是夏季约一两点、柔和暖白日光、克制体积光与稀疏微尘；不要擅自把光线改成四五点的低太阳，也不要仅为提亮而移动光斑和改变斜影。
- 窗帘开、合是两个需要分别验收的照明状态。闭帘应保留柔和透光与参考色调，开帘应恢复窗外绿树和直射光；两态都不得与柜体、窗框或晾衣杆穿模。

### 4.5 交互和浏览器整合

- Blender 负责持久几何、材质、形态键和交互枢轴；浏览器负责实时灯光、微尘、水流、输入、碰撞、UI 和状态动画。不要为了“全部 Blender”把需要实时变化的浏览器效果烘死。
- 每个可点击物件必须有稳定名称或 refs、正确拾取范围、明确枢轴和往返状态；拖动环顾不能误触点击。
- 新房间至少复用并回归现有交互契约：墙壁开关、窗帘、花洒、水龙头和反锁旋钮可往返；水流只在开启时更新；复位按钮释放后不得保持高亮。
- 自由探索支持 WASD/方向键，Shift 上升、Ctrl 下降；手机左下方向键、右下竖排升降；沉浸模式保留移动键并隐藏非必要组件。
- 保持极小碰撞体积的探索体验，同时使用真实表面和局部障碍，允许穿过桌下、床下等真实空隙。不要退回贯穿全高的粗矩形碰撞。
- 全屏必须测试“进入房间 → 退出到首页 → 再次进入房间”的完整循环；返回后入口按钮必须可点击。首次载入应显示统一的进度条和可恢复错误状态。
- 首页不显示“空间总览”入口；房间内部仍保留“自由探索”“空间总览”和复位。不要恢复已经删除的快捷地点、实时位置提示或独立灯光/窗帘屏幕按钮。

**交互音效**

交互音效由 `room-site/dist/room-audio.js` 管理，首次打开网页必须默认静音，不持久化取消静音的选择。仅用户点击右上角声音按钮后创建/恢复 AudioContext 并加载本地录音；沉浸模式保留此按钮，不恢复“对照照片”。柜门使用短促、低音量、削弱尖锐高频的轻声开门实录，每次运动仅播放一次且保持原音调；禁止循环长吱声或随门速降调。开合采用较缓的指数缓动（系数 3.1），声量跟随实际速度，阻挡或静止时停止；窗帘仍使用轻量合成布料声。水龙头和花洒分别使用独立的 CC0 水柱落盆、喷淋实录，不能退回同一段噪声仅改滤波。`room-site/tools/prepare-room-audio.py` 裁取稳定段并交叉混合循环接缝，导出无编码延迟的单声道 PCM；来源和许可见 `dist/assets/audio/sources.json`。水声跟随当前水流状态，不能在每帧创建重复声源。首页、后台暂停并清理声源，返回房间按当前状态恢复。修改后运行 `tests/audio.mjs` 和 `tests/wardrobe.mjs`，检查默认静音、独立持续水声、循环接缝、开合速度、暂停/重入及加载失败重试。

### 4.6 自然微风：已验证的可复用做法

用户已认可 `breeze28` 的微风效果，实现基线为 Git 提交 `f1b0fd2`。新房间优先复用其方法，再按实际尺寸、窗户方向和默认观察距离校准。相关代码为 `room-site/dist/room-breeze.js`（风动）、`sunlit-dust.js`（微尘）、`soft-daylight.js`（渲染缓存）和 `main.js`（调度）。不要仅为了添加微风重新建模或覆盖 Blender 工程。

**观感与受力关系**

- “呼吸感”来自柔软、轻薄物体受到同一阵微风的响应：枝叶缓慢摆动，叶片局部轻转，窗帘宽幅轻鼓、下摆轻移，花梗连带花头微动，阳光中的微尘缓慢漂移。柜子、桌子、篮体、杯子和其他硬物保持静止，不能整体缩放、漂浮或同步摇摆。
- 共用一个连续时间和主风向，叠加不同周期的缓慢阵风；树木按个体、叶片按实例位置错开相位，避免整排树机械同步，也不要每帧重新随机抖动。当前阵风包络为 `0.53 + 0.21*sin(t*0.23) + 0.12*sin(t*0.071+1.2)`，这里的系数是时间相位系数，不是 Hz。
- 室内响应比室外弱，并延迟约 1.4 秒；窗帘开合连续调节室内风力。树干根部、叶柄连接点、花梗入篮处及窗帘顶部/侧缘要有固定或平滑衰减区。窗帘向房间内轻鼓，左缘接柜不露缝，不能向后穿进飘窗和柜体。
- **自然不等于看不见。** 旧版闭帘鼓动约 2 毫米，花朵又被短距离衰减削弱，默认视角几乎看不到。必须在正常缩放、默认视角静看一个完整的缓慢起伏过程；先确认加载新版本，再调整幅度与衰减，不能只凭时间计数增长就认定效果达标，也不能靠增加灰尘浓度来制造动感。

**幅度与节奏参考（米制场景）**

以下是 `breeze28` 的基础系数，实际位移还乘阵风强度、固定点权重、室内系数、距离衰减和波形；不是恒定位移，也不是要求所有房间照搬的最终振幅。

| 对象 | 当前基础系数 / 处理 | 校准重点 |
| --- | --- | --- |
| 树冠枝条 | 主风向位移系数 `0.11 m`，两层慢波叠加 | 根部固定，枝梢更明显；保持原树形、单叶大小和树间差异 |
| 叶片 | 叶柄处局部旋转系数 `0.23 rad`，叠加所在枝条的位移 | 保留实例，叶片法线随局部旋转，不能与枝条脱节 |
| 窗帘 | 室内方向 `0.10 m`、横向 `0.040 m`、竖向 `0.012 m`；主相位 `t*0.82` | 宽幅缓慢鼓动，不增加密集褶皱；收拢时横摆减半，顶部和两侧固定 |
| 花梗与花头 | 位移系数 `0.016 m`，随高度平方增长 | 花瓣、花心、花梗用同一连续位移场；篮体、把手保持原位 |
| 微尘 | 同主风向的有界漂移系数 `0.032 m`，叠加原有细小浮动 | 保持原数量、透明度和仅在日光中显现的规则，不变成满屋飞絮 |

- 当前窗帘室内系数为 `0.68 + 0.32*openness`，干花为 `0.42 + 0.58*openness`，`openness` 从闭合的 0 连续过渡到打开的 1。闭帘仍应有可辨认、较弱的布料微动。
- 干花在当前房间内保持完整风力，6 米范围的最后 1.5 米才衰减。所有花组件用共同的花篮原点计算衰减，不能分别按各自包围盒中心计算，否则会出现花头与花梗断开。新房间按观察距离重设范围，避免默认视角就衰减到不足一个像素。

**实现与性能边界**

- 使用 GPU 顶点着色器在已加载的 Blender 网格上叠加位移，不每帧在 CPU 重建几何或上传数万片叶子的实例矩阵。风动对象须在静态合批前标记并保留；只克隆需要动画的材质，接续原有 `onBeforeCompile` 和缓存键，不能丢掉窗帘透光等已有材质处理。
- 窗帘位移在闭合/收拢形态键求值后、投影前叠加；实例叶片和花朵先在共同坐标系计算风场，再正确转换回实例局部坐标。新房间必须重新确认固定点、局部轴和非均匀缩放；不能照抄永旺家园的节点名、窗帘边界、叶柄坐标或高度阈值。
- 相机静止时缓存静态不透明场景的 **HDR 颜色与深度**，恢复两者后仅重绘风动物体、透明叠加物和微尘，再合成。透明窗玻璃必须与动态枝叶在这一重绘阶段正确排序；不能把玻璃及其深度烘进静态底图，否则后方树叶会消失。不能只叠加新颜色而不恢复深度，造成遮挡错误和拖影。
- 相机、灯光、窗帘开合、交互状态、视口尺寸或风动物体可见集合改变时更新缓存；只推进风动时间不能触发整个房间和阴影贴图每帧重绘。保留当前太阳位置、静态树影代理、光斑、色调和丁达尔光束；静止视角复用已有泛光与体积光。这是轻微风动的性能折中，不用于大幅变形或要求精确动态投影的场景。
- 当前动画更新上限为桌面 30 次/秒、粗指针设备 24 次/秒，初次约 2 秒渐入；这是更新节奏，不是对实际帧率的保证。暂停时不累计动画时间，恢复时限制单次时间步长，避免突然跳动。
- 页面隐藏、返回首页、空间总览及系统减少动态效果时暂停；闭帘且从室内观察时停止被遮住树木的重绘，进入飘窗或开帘后恢复。视锥和距离裁剪需考虑最大位移余量，不能让枝梢或窗帘边缘突然消失。

**验收与排错顺序**

1. 核对入口模块缓存版本及 `get_room_view` 的 `breeze.version`，确认浏览器加载当前代码；不要反复增大参数来补偿旧缓存。
2. 在默认视角分别静看闭帘下摆和开帘树叶，再近看花梗与花头的连接；检查所有开合中间状态、柜边、帘顶、叶柄和篮口固定点。远看可察觉、近看不夸张才算通过。
3. 移动、旋转、复位、切换总览和恢复后台页面，检查透明玻璃后树叶、动态边缘遮挡、拖影、光斑和丁达尔效果。桌面与手机视口都要检查；不能仅看两张静态截图或诊断计数。
4. 用 `frameCache` 和 draw call 观察静止视角是否复用静态缓存；确认无新增着色器、WebGL 或资源错误。修改实现后运行 `tests/breeze.mjs`、`tests/daylight-cache.mjs` 和 `npm run test:scene`，验证缓冲不变、组件连接、透明排序、缓存失效及暂停恢复；自动检查不能替代实际画面验收。

## 5. 几何与接触质量标准

- 所有有体积的物件都应有合理厚度：窗帘、桌布、花瓣、帽檐、头盔壳与镜片、软垫、被子、杯壁、洗手盆、马桶圈等不得用无厚度纸片代替近景实体。
- 软物件需要连续曲面、有限且自然的褶皱、轻微不对称和受重力影响的接触，不要使用过多均匀正弦波或锋利折线。
- 连接关系必须成立：软管连到两端、空调管线贴墙入孔、花篮把手两端入扣、栏杆入墙、门楣遮住缝、头盔和摆件脚底接触承托面。悬空、断开和相互穿透都属于阻断发布的问题。
- 可动件要检查整个运动区间，不只检查一个端点。窗帘、门窗、旋钮、活动花洒等至少采样若干中间状态，并计算与邻近物件的最小间距。
- 物体变换、法线和面朝向应正确；需要平滑的曲面使用一致法线，硬边保留受控倒角。避免因负缩放、未应用变换或翻面造成黑面和三角阴影。
- 地面必须避免近共面表面。瓷砖与砖缝可保留真实倒角和独立材质，但砖缝应只占实际缝隙，不得在整片瓷砖下铺一张相差不足毫米的平面。贴花和标识要用 polygon offset、足够几何偏移或合并进材质，防止 Z-fighting。
- 透明面、玻璃、叶片和布料要明确双面、深度写入和投影策略；不要让整块玻璃投下不透明实心阴影。

## 6. 模型源码与导出资产契约

- `.blend` 是可编辑模型源；`scene.json + geometry.bin + geometry.bin.gz` 是网页加载包。三者必须来自同一次导出，压缩和未压缩二进制解压后应完全一致。
- 完整模型包格式保持 `blender-room-pack-1`。每次发布都更新 `scene.json` 的 revision，并同步更新引用模型包或模块的缓存版本；至少检查 `room-site/dist/index.html`、`archive.js`、`main.js`、`blender-room.js` 及本次涉及的模块。不要出现 HTML 使用新脚本而脚本仍请求旧模型包的混合缓存。
- 网格至少保存 position、normal、UV；需要烘焙的网格还要保存 UV1。索引必须在顶点范围内，法线为有限值且近似单位长度，包围盒和三角形数量写入检查报告。
- 导出器必须集中处理坐标轴、单位、矩阵、法线逆转置和形态键。不要在网页中为每个错误模型添加临时旋转、缩放或偏移补丁。
- 交互 refs、节点 ID、材质 ID 和 shape key/morph 的语义属于运行时契约。精修现有模型时尽量原位替换目标描述符并断言其他节点、材质、纹理未变；如必须改 ID，同步更新所有引用和测试。
- 稳定静态物件按材质合批；大量树叶、重复栏杆等优先共享网格实例。合批前保留需单独点击、动画、显隐或不同投影策略的对象。
- 保留真实厚度和轮廓的前提下控制面数。优先把细分用于默认视点近处、高曲率轮廓和镜面物件；远景叶片、窗外建筑和不可见背面可以简化。不要只看总三角面数，也要检查 draw call、透明排序、阴影投射体数量、纹理尺寸和移动端像素密度。
- 每个持久修改应具备以下至少一种可重现来源：独立 `.blend`、可从父 revision 运行的 `build-*`/`refine-*` 脚本，或在完整工程中明确保存的编辑结果。纯手工改完只导出二进制而不保存源文件，不可接受。

## 7. Git 版本管理和修改顺序

- 用户已明确改用 Git 管理版本，历史备份压缩包已删除；禁止再为修改创建 ZIP、TAR、7z 等压缩快照。运行时的 `geometry.bin.gz` 和软件安装包不属于版本备份。
- 修改完整 `.blend`、模型包或关键运行时前检查 `git status` 和父提交，确保将被覆盖的当前有效版本已入库；必要时创建范围明确的检查点提交。修改完成并验证后提交相关源文件与导出物，提交说明写明行为变化和验证结果。
- 不把无关改动、私有参考图、日志或临时文件一起提交；不自动清空用户改动、不使用破坏性的 reset。需要回退时定位已知提交，按文件恢复目标内容。
- 先记录父 revision，再执行修改；新 revision 应表达阶段，而不是覆盖父版本名字。脚本的输入断言和输出 revision 必须与记录一致。
- 新房间建立独立生成链，避免复制一长串永旺家园的历史补丁。基础生成器应能从明确输入生成结构版；后续精修脚本按少量、职责清晰的阶段串联。
- `room-site/tools/capture-room-for-blender.mjs` 仅用于离线保留旧网页场景的尺寸和贴图，不是常规建模入口。

## 8. 最小验收门槛

每一阶段都只运行与改动相称的检查，但以下项目在发布完整房间前必须全部通过。

### 8.1 Blender / 数据检查

- 工程可由官方 MCP 无缺失文件打开；源图路径可解析，场景单位和轴向正确。
- 所有导出 position、normal、UV、UV1、index 均为有限值；索引不越界；压缩包可解压且与原 binary 一致。
- 检查目标物件的尺寸、包围盒、最低点/接触面和与邻物最小间距；软物件和可动件检查中间状态。
- 使用前后清单或断言确认未修改无关节点、材料、纹理、灯光参数和交互 refs。
- 检查近共面、重复面、翻面、非预期零面积三角形、悬空、断管、穿模和不可见大面积投影代理。

### 8.2 代码和静态资源检查

- 对所有改过的 JavaScript 运行 Node 语法检查；对构建/精修脚本运行相应静态检查。
- 验证 `scene.json` revision、模块缓存版本和模型包相互一致。
- 保留首页稳定标记 `<!-- beipiao-room-app:v1 -->`。本地和公网启动器通过它识别房间页面；改 UI 时不得删除或改写。
- 不要从 `file://` 打开网站验证。必须通过本地 HTTP 服务测试资源、模块、压缩和缓存行为。

### 8.3 真实浏览器检查

- 逐一查看入口、床边、桌面、窗边、卫生间、重点纪念物和空间总览；近看检查材质和接触，远看检查比例、灯光和闪烁。
- 移动和旋转相机，确认地砖、贴花和透明面不发生明显 Z-fighting；开合窗帘后再次检查色调、穿模和光束。
- 实际点击全部交互并往返，检查拖动不误触、复位状态、自由探索/总览切换和沉浸模式。
- 桌面端测试普通窗口和全屏往返；移动端至少测试一个竖屏和一个横屏尺寸，检查左侧方向键、右侧竖排升降和安全区。
- 清空或检查控制台，不能新增 JavaScript、着色器、资源 404、纹理或 WebGL 错误。
- 性能结论必须基于实际浏览器观测。可以说明已减少 draw call 或资源大小，不得宣称所有设备固定帧率。

可提交的回归统一放在 `tests/`。至少保留并运行几何/资源检查、`tests/viewer-lifecycle.mjs`、`tests/test-start-room.ps1` 和聚合入口 `tests/check.ps1`（以仓库实际文件名为准）。遗留在 `analysis/` 的阶段检查只可作为迁移参考；它们可能绑定旧 revision 或本机路径，必须先移植输入假设，不能让新克隆直接依赖旧盘符，也不要为“通过测试”而删掉有效断言。

## 9. 运行和启动器契约

- 当前正式托管为 Cloudflare Pages 项目 `beipiao`，已连接 `LeonZ03/BeiPiao` 的 `main` 分支；推送会自动部署。正式地址为 `https://room.leonz03.dpdns.org`，备用地址为 `https://beipiao-4op.pages.dev`，根域名不绑定网站。自动部署、HTTPS 和线上加载已于 2026-09-20 验证。后续发布须核对 Pages 对应提交部署成功，再验证线上页面；交付时检查工作区及远程同步状态，如有未提交或未推送内容应如实说明，不能仅凭推送成功宣称部署完成。

- Cloudflare Pages 使用 `npm run build:pages` 生成 `.pages-dist/`，只部署该目录。`tools/build-pages.mjs` 保留模型描述符，将原始及 gzip 二进制分别拆为最多 8 MiB、含内容哈希的分块；`room-binary.js` 兼容线上分块和本地单文件。不要降低模型精度来绕过单文件限制，也不要把 `.blend`、参考图或整个仓库发布到网站。运行 `npm run test:pages` 核对合并后字节一致、原始格式回退、下载进度及错误处理；分块使用长期缓存，其余资源重新验证缓存。源场景 revision 不因仅修改传输封装而变化。

- 面向普通用户的首选入口是仓库根目录 `启动房间.cmd`。首次克隆可先运行 `local-access/init-room.ps1`，或使用可选的 `make init`；初始化只准备便携运行时和必要依赖，不修改房间资产。之后双击启动器即可调用 `local-access/start-room.ps1`，启动本地 HTTP 服务、自动打开浏览器，并在可用时创建和验证 Cloudflare 临时链接。
- 关闭启动器的 PowerShell 窗口，应同时结束它创建的本地服务和 Cloudflare 进程；不要把浏览器纳入该进程组。
- 本地和公网健康检查都必须验证真实房间首页，而不是只看端口或 Cloudflare 注册成功。公开链接可能因 DNS、代理或网络环境不可达；应保留本地服务并给出可诊断日志，不能把候选 URL 冒充已验证地址。
- 改启动器后运行 `tests/test-start-room.ps1` 或 `make check`。默认测试不应创建公网隧道；只有得到相应授权并显式传入隧道测试参数时才连接 Cloudflare。
- 初始化脚本应准备 Node.js 22+ 与 cloudflared 的便携运行时，重复执行仍安全，并验证文件完整性。启动器优先接受 PATH 和标准 Windows 安装位置中合适的工具；缺少时使用或下载项目便携运行时。测试可通过 `BEIPIAO_PREFER_PORTABLE=1` 强制验证便携路径。
- 可选命令入口为 `make init`、`make start`、`make local` 和 `make check`；Windows 用户不安装 make 也必须能通过 PowerShell 初始化和双击启动。
- 不得在脚本中依赖开发者电脑的盘符、用户名、固定软件安装目录或遗留 D 盘参数文件。发现旧脚本含绝对路径时，先改为仓库相对路径再执行，不能通过创建同名目录或复制本机环境掩盖问题。

## 10. 永旺家园当前不可回退的已确认约束

维护当前房间时，除非用户再次更正，保留以下结果：

- 床头靠入口侧墙，躺下朝窗；粉色窗墙和书桌墙、左侧灰墙；衣柜在窗左，桌下暖气，空调在右墙高处。
- 右上活动窗位于防护横杆的室内侧，绕右侧铰链向室内微开；横杆只在右上活动窗后方，两端直接嵌墙，没有外露托座、竖边或覆盖其他固定玻璃的杆件；窗扇与晾衣杆全程分离。
- 窗外是夏季绿树；树叶数量形成繁茂感，但单叶大小、绿色层次和投影保持自然。既有太阳、微尘和丁达尔效果不能因改树而被破坏。
- 床上只有一个居中枕头；被子有厚度，窄于床垫且不垂地。椅子、球拍网、头盔、GSX250R、桌面物件、门饰和卫浴以现有精修模型为基线。
- 头盔侧面没有 LS2 字样，`FF801 CARBON` 位于侧面下沿；头盔底缘接触柜顶。高达身体、枪盾、姿势和脚底接触已认可，头部现为上一版本的 80%。
- 闭帘左缘接到衣柜边，不能露出贯穿高度的亮缝；窗帘保持较少、较宽、柔和的褶皱和暖象牙提花质感。
- 卧室和卫生间瓷砖保留现有颜色、倒角、反光和细缝；砖缝使用仅位于缝隙的连续网格，不恢复瓷砖下的近共面整片填缝面。
- 浏览器继续保留实时午后灯光、低浓度体积光、微尘、水流、交互和移动；不要把这些动态功能替换为静态 Blender 渲染图。
- 用户已认可 `breeze28` 的微风幅度与节奏，保留三棵树的枝叶、提花窗帘、干花和微尘之间的自然联动；具体实现、参数参考、性能约束及排错流程见第 4.6 节，不能退回默认视角几乎不可见的旧幅度。
- 衣柜按已确认的 v2 实拍结构：左两门共用宽柜腔（上格、挂衣杆、下层板），右单门四格且内侧有长镜，暂时全部留空；侧板、球拍、帽子和顶上头盔固定不动。完整精修链为 `viewer26 → refine-wardrobe.py → wardrobe29 → refine-wardrobe-hinges.py → wardrobe29b`，必须经官方 MCP 执行。
- 活动柜门使用 Blender 导出的独立枢轴；写出 `matrix_local` 前先刷新依赖图，并用浏览器世界坐标核对，避免父坐标未求值使枢轴落到原点。柜门与窗帘必须独立点击、独立开合，不自动操作另一物件；拾取使用实际可见网格，不能用覆盖两者的大包围盒。门变化必须同步碰撞包围盒、阴影、地板与门镜反射及静态缓存。两个平面反射共用递归保护，捕获微风静态底图期间不更新镜像。镜面使用较低分辨率（桌面 256×512，手机 128×256），移动视角每个实际渲染帧更新，静止时缓存；不要通过跳过移动帧制造倒影卡顿。相关回归为 `tests/wardrobe.mjs`。

## 11. 完成一次修改时的交付说明

### 窗外精修链与复用边界

- 外景精修链为 `wardrobe29b → refine-exterior-trees.py → exterior31a → refine-exterior-buildings.py → exterior31 → refine-exterior-finish.py → exterior31c`，脚本共用 `exterior-authoring.py`，均通过官方 MCP 执行。父 revision 不匹配时不得直接重放。
- 网页验收后的性能精修为 `exterior31c → refine-exterior-leaf-shading.py → exterior31e → refine-exterior-far-budget.py → exterior31f`。叶片以轻量漫反射及背光代替逐像素复杂环境高光；远楼保留窗洞、基础色和遮蔽，省略亚像素凹凸与阴影范围外的接收采样。不要恢复这些无明显收益的开销。
- 后续修正链为 `exterior31f → refine-exterior-alignment.py → exterior32`。建筑附件设父级后，必须刷新依赖图再导出 `matrix_local`，并核对网页世界坐标与 Blender `matrix_world`；旧导出曾让 60 个窗洞背板/窗台偏离楼体 2.8 米。全部 43 扇窗的 86 个附件用 `exteriorWindowGlass` 关联，回归必须覆盖侧视连接，而不只检查一扇窗洞深度。
- Blender 字节缓冲图像的像素应直接写入 sRGB 编码值，再标注 sRGB；不要先转线性再存入该缓冲，否则 PNG 被重复压暗。`exterior-authoring.py` 已修正此行为，新贴图发布前应重新读取采样检查。保留旧贴图供历史资产引用；不为修正墙色重放树木或改变室内曝光。当前院内行走面为灰色水泥，土色仅用于树池；墙体为浅灰、米白，不能恢复深褐立面。
- `exterior32 → refine-window-glints.py → sunlight33` 调整近处三块飘窗玻璃的高光，避免室内补光灯形成树丛前的孤立光团。薄透明玻璃保留深度测试、关闭深度写入，让后方实体深度参与太阳遮挡。`soft-daylight.js` 的天空日轮使用与室内太阳一致的方向、约 0.53° 角直径及克制光晕，仅对天空深度像素合成；不能为了露出太阳改变已认可的斜影、把光团贴到叶片上，或使用穿过楼体的屏幕光晕。仍在微风合成时按最新叶片深度遮挡。
- 后续太阳可见性修正为 `sunlight33 → refine-sun-sightline.py → sunview34`：用户明确批准疏开日轮方向的 203 片叶子，现保留 48,209 片；枝干、剩余叶柄、微风和室内投影代理不变。日轮改为参与深度测试的 HDR 圆盘，随相机平移保持方向与角直径不变，不产生额外照明；天空调色在泛光之前执行，并保留日轮 HDR 亮度，避免被背景色覆盖。天空使用仅作用于天空深度的浅蓝渐变和克制日晕，不能只在无树测试场景验证太阳；须在实际开帘、靠近玻璃的窗边视角看到日轮。`tests/exterior.mjs` 同时检查日轮中心及边缘无遮挡，`tests/daylight-cache.mjs` 检查日轮深度和缓存语义。
- 三棵树保留上述 48,209 片叶子及原有投影代理；每棵树共享一款有曲率的叶片（11 个共享顶点、12 个三角形）。导出应按位置、法线、UV 等完整属性合并重复角点，不能只减少三角形却把风动顶点数翻倍。叶柄与枝条使用同一空间变形场，树冠遮蔽写入实例色。
- 建筑窗洞为真实凹槽，43 扇窗共享环境反射贴图；不为各窗增加实时反射。外景材质单独标记 `exteriorSurface`，由 `exterior-surface.js` 控制远景雾化与叶片背光，不改变全局曝光、太阳或体积光。飘窗玻璃使用单独的低不透明度材质，避免暖白漫反射覆盖近树。
- `soft-daylight.js` 仅根据最终表面的世界深度减弱窗外像素的雾光覆盖，室内表面的光束强度保持原基准。勿通过全局降低体积光消除窗外泛白。新材质解析只接收对应 Three.js 材质类型实际支持的颜色属性，不能向 MeshBasicMaterial 写入 emissive 导致缺失 uniform。
- `tests/exterior.mjs` 检查窗洞深度、共享网格/反射、叶片法线与风动兼容；`measure_room_rendering` 为当前视角约 5.5 秒的移动测量工具，测量结束恢复朝向。对比必须使用同一设备、视口、视角及窗帘状态，不能把静止缓存帧与移动完整帧混比。

向用户汇报时只说明：改了什么、为何这样改、验证了什么、还存在哪些由资料不足造成的限制。给出真实文件或预览入口，不堆砌内部执行日志。若某项尺寸来自照片估计，明确说是估计；若只在 Blender 中检查过，不要说网页已经验证。任何“完成”“还原”“无穿模”“运行流畅”的结论都必须与已执行的检查范围一致。
